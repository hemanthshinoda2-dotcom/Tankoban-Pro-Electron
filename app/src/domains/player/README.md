# Player domain (Qt-only)

Tankoban Pro uses an external Python/Qt player (`app/player_qt/`) launched from the Electron renderer via `Tanko.api.player.launchQt(...)`.

The older embedded/canvas player adapter has been removed.
