diff -ruN orig/app/src/domains/video/video.js src/app/src/domains/video/video.js
--- orig/app/src/domains/video/video.js	2026-01-31 05:00:23.000000000 +0000
+++ src/app/src/domains/video/video.js	2026-01-31 05:41:16.196693431 +0000
@@ -140,6 +140,9 @@
     videoInfoBtnTop: qs('videoInfoBtnTop'),
     videoDiagnostics: qs('videoDiagnostics'),
 
+    // BUILD 101: Fullscreen HUD exit arrow
+    videoExitFullscreenBtn: qs('videoExitFullscreenBtn'),
+
     videoCtxMenu: qs('videoCtxMenu'),
     // Quick tools row additions (Build 24 prompt 4)
     videoSubDelayBtn: qs('videoSubDelayBtn'),
@@ -533,7 +536,7 @@
     mark.style.cursor = 'pointer'; // BUILD 87: Indicate clickable
     
     // BUILD 89 FIX 2: Force bright red and high z-index for visibility
-    mark.style.background = '#FF0000';
+    mark.style.background = '#FFFFFF';
     mark.style.width = '2px';
     mark.style.zIndex = '9999';
     mark.style.position = 'absolute';
@@ -7020,6 +7023,13 @@
     // el.videoFullscreenBtn?.addEventListener('click', () => { toggleFullscreen(); showHud(); });
     el.videoFullscreenBtnTop?.addEventListener('click', () => { toggleFullscreen(); showHud(); });
 
+    // BUILD 101: Fullscreen HUD exit arrow uses the existing fullscreen toggle path.
+    el.videoExitFullscreenBtn?.addEventListener('click', (e) => {
+      e.stopPropagation();
+      toggleFullscreen();
+      showHud();
+    });
+
 // BUILD 65: Control bar buttons removed - functionality in context menu
 // el.videoMuteBtn?.addEventListener('click', () => {
 //       if (volPanelOpen) closeVolPanel();
diff -ruN orig/app/src/index.html src/app/src/index.html
--- orig/app/src/index.html	2026-01-31 03:36:53.000000000 +0000
+++ src/app/src/index.html	2026-01-31 05:39:52.511173977 +0000
@@ -350,6 +350,9 @@
 
             <div id="videoDiagnostics" class="videoDiagnostics hidden" aria-live="polite"></div>
 
+            <!-- BUILD 101: Fullscreen exit arrow (only visible when fullscreen + HUD shown) -->
+            <button id="videoExitFullscreenBtn" title="Exit fullscreen" aria-label="Exit fullscreen" type="button">←</button>
+
             <!-- BUILD64: Comprehensive right-click context menu (VLC/PotPlayer baseline) -->
             <div id="videoCtxMenu" class="videoCtxMenu hidden" role="menu" aria-label="Video menu">
               <button class="ctxItem" data-act="togglePlay" role="menuitem">
diff -ruN orig/app/src/styles.css src/app/src/styles.css
--- orig/app/src/styles.css	2026-01-31 03:37:05.000000000 +0000
+++ src/app/src/styles.css	2026-01-31 05:40:21.765006622 +0000
@@ -1815,12 +1815,12 @@
   position:absolute;
   top:0; bottom:0;
   width:2px; /* BUILD 89: Wider for visibility */
-  background: #FF0000; /* BUILD 89: Bright red for visibility */
+  background: #FFFFFF; /* BUILD 89: Bright red for visibility */
   z-index: 9999; /* BUILD 89: High z-index to ensure visibility */
   pointer-events: auto; /* BUILD 89: Ensure clickable */
 }
 .videoScrub--minimal .scrubChapterMark{
-  background: #FF0000; /* BUILD 89: Bright red override */
+  background: #FFFFFF; /* BUILD 89: Bright red override */
   width: 2px; /* BUILD 89: Wider for visibility */
   z-index: 9999; /* BUILD 89: High z-index */
 }
@@ -2666,9 +2666,42 @@
   pointer-events:auto;
 }
 
+/* BUILD 101: Fullscreen exit arrow (top-left, unobtrusive). */
+#videoExitFullscreenBtn{
+  position:absolute;
+  top:10px;
+  left:10px;
+  z-index:6; /* Above video, below menus */
+  width:28px;
+  height:28px;
+  border-radius:8px;
+  border:1px solid rgba(255,255,255,0.18);
+  background:rgba(0,0,0,0.18);
+  color:rgba(255,255,255,0.88);
+  font-size:16px;
+  line-height:1;
+  display:grid;
+  place-items:center;
+  opacity:0;
+  pointer-events:none;
+  cursor:pointer;
+  transition:opacity 140ms ease, background 140ms ease;
+  backdrop-filter: blur(4px);
+}
+
+body.videoFullscreen .videoStage.showHud #videoExitFullscreenBtn{
+  opacity:0.55;
+  pointer-events:auto;
+}
+
+body.videoFullscreen .videoStage.showHud #videoExitFullscreenBtn:hover,
+body.videoFullscreen .videoStage.showHud #videoExitFullscreenBtn:focus-visible{
+  opacity:0.9;
+  background:rgba(0,0,0,0.28);
+}
+
 /* Minimal play button at far left */
-.minimalNavBtn{ width:34px; height:34px; border-radius:999px; border:1px solid rgba(255,255,255,0.18); background:rgba(0,0,0,0.25); color:inherit; display:grid; place-items:center; margin-right:8px; }
-.minimalNavBtn:last-child{ margin-right:0; margin-left:8px; }
+.minimalNavBtn{ width:34px; height:34px; border-radius:999px; border:1px solid rgba(255,255,255,0.18); background:rgba(0,0,0,0.25); color:inherit; display:grid; place-items:center; margin-right:0; }
 .minimalNavBtn:hover{ background:rgba(255,255,255,0.10); }
 .minimalNavBtn:disabled{ opacity:0.4; cursor:not-allowed; }
 
@@ -2681,17 +2714,14 @@
   color:rgba(255,255,255,0.9);
   font-size:14px;
   cursor:pointer;
-  border-radius:0;
+  border-radius:4px;
   display:flex;
   align-items:center;
   justify-content:center;
   transition:background 120ms ease;
   padding:0;
   margin:0;
-  border-top-left-radius:0;
-  border-bottom-left-radius:0;
-  border-top-right-radius:4px;
-  border-bottom-right-radius:0;
+  /* Keep symmetric corners in minimal HUD */
 }
 
 .minimalPlayBtn:hover{
